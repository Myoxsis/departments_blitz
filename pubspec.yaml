author: you
name: departments_blitz
description: Departments Blitz — ad‑first quiz (no Firebase)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.1.0
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  test: ^1.24.0

flutter:
  uses-material-design: true
  assets:
    - assets/departments.json
